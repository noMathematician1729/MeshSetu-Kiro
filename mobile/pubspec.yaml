name: meshsetu_mobile
description: MeshSetu mobile app - Developer A's mesh transport logic (Flutter/Android).
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  protobuf: ^6.0.0
  cryptography: ^2.7.0
  collection: ^1.19.0
  fixnum: ^1.1.1
  universal_ble: ^2.1.1
  flutter_secure_storage: ^11.0.0
  flutter_foreground_task: ^10.0.0
  permission_handler: ^13.0.1
  device_info_plus: ^13.2.0
  crypto: ^3.0.7
  path_provider: ^2.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.25.0
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
