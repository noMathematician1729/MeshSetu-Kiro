import { implemented, next } from '../content';
import SectionLabel from './SectionLabel';

export default function Prototype() {
  return <section className="prototype section-paper" id="prototype"><div className="container"><div className="section-heading section-heading--split"><div><SectionLabel>Current build</SectionLabel><h2>A working foundation, moving toward <em>field validation.</em></h2></div><p>MeshSetu is an honest hackathon prototype: the core transport is real, while the physical field path and a few higher-level integrations still need proving.</p></div><div className="status-columns"><article className="status-column status-column--done"><div className="status-column__head"><span className="status-icon">✓</span><div><small>IMPLEMENTED + LOCALLY VERIFIED</small><h3>Foundation in place</h3></div></div><ul>{implemented.map((item) => <li key={item}>{item}</li>)}</ul></article><article className="status-column status-column--next"><div className="status-column__head"><span className="status-icon">→</span><div><small>INTEGRATION WORK STILL REQUIRED</small><h3>Next proof points</h3></div></div><ul>{next.map((item) => <li key={item}>{item}</li>)}</ul></article></div><div className="prototype-note"><span>HACKATHON PROTOTYPE</span><p>Not a certified emergency service. MeshSetu is an application-layer BLE overlay, not Bluetooth SIG Mesh.</p></div></div></section>;
}
