# MeshSetu Landing Page

Standalone React/Vite landing page for the MeshSetu hackathon prototype. It is intentionally separate from the Flutter mobile app and Python dashboard.

```bash
cd landing-page
npm install
npm run dev
```

Build a production bundle with `npm run build`.
